amends "package://github.com/jdx/hk/releases/download/v1.44.2/hk@1.44.2#/Config.pkl"
import "package://github.com/jdx/hk/releases/download/v1.44.2/hk@1.44.2#/Builtins.pkl"

local message = new Mapping<String, Step> {
  ["check-conventional-commit"] = Builtins.check_conventional_commit
}

local builtins = new Mapping<String, Step> {
  ["check-case-conflict"] = Builtins.check_case_conflict
  ["check-merge-conflict"] = Builtins.check_merge_conflict
  ["end-of-file-fixer"] = Builtins.newlines
  ["trailing-whitespace"] = Builtins.trailing_whitespace
  ["yamlfmt"] = Builtins.yamlfmt
}

local local_hooks = new Mapping<String, Step> {
  ["codespell"] {
    glob = List("*.tra", "*.tp2", "*.d", "readme.md")
    exclude = List("hk.pkl")
    check = "codespell -c {{files}} --config .codespellrc"
    fix = "codespell -cw {{files}} --config .codespellrc"
  }
  ["check-ie"] {
    glob = List(".*.bam",".*.baf",".*.cre",".*.d",".*.itm",".*.spl",".*.sto",".*.wav",".*.vvc")
    check = "scripts/check-ie.sh {{files}}"
  }
  ["git-check"] {
    check = "scripts/check-git.sh"
    fix = "git add --renormalize ."
  }
  ["weidu-check"] {
    glob = List(".*.d",".*.tp2",".*.tpa",".*.tpp")
    check = "scripts/check-weidu.sh {{files}}"
  }
  ["no-uppercase-check"] {
    check = "test $(find k0_iskp -type f -name '*[[:upper:]]*' | wc -l) -eq 0"
    fix = "scripts/fix-case.sh"
  }
  ["nix fmt"] {
    fix = "nixfmt flake.nix"
  }
}

hooks {
  ["commit-msg"] {
    steps {
      ...message
    }
  }
  ["pre-commit"] {
    fix = true
    stash = "git"
    steps {
      ...builtins
      ...local_hooks
    }
  }
}
